include _header

  h1 Thank you 
    if(name)
      |#{name} 
    |for standing for forests

  p Hi there, your order on Stand For Trees has been completed.

  h3 Order #: #{order}
  h3 Date: #{date}


  table.pTable
    tr
      th Obtained Badge
      td #{type}
    tr
      th Total Price
      td #{price}
    if(tonnes)
      tr
        th Tonnes Offset
        td #{tonnes} CO2e
    if(issued)
      tr
        th Issued Date
        td #{issued}
    if(expiry)
      tr
        th Expiry Date
        td #{expiry}

  if(report)
  table.pTable
    tr
      th Downloadable
      td Detailed Emissions Report


  if(projects)
    table.pTable
      tr
        th Projects Supported
        th.right Complimentary Items
      each project in projects
        tr
          td 
            p.left #{project.name}
            p.left #{project.country}

          if(project.bundle)
            p.right Images Bundle
          if(project.extras)
            each extra in project.extras
              p.right #{extra}

  p.orderN You can access your badge details and donwloadables by login with your badging program account details 
    a.purchase(href=accountUrl target="_blank") here

  if(discount)
    p.orderN For your purchase you're entitled to a rebate code of $#{discount.value} on your next Business Calculator Badge: 
    code #{discount.code}

  p.support Thank you for your support.

  include _footer